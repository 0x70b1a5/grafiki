<%= react_component("Bounty", {
  title: @bounty.title,
  artist: @bounty.artist,
  description: @bounty.description,
  pic: @bounty.pic,
  amount: @bounty.amount,
  patron: @bounty.patron,
  lat: @bounty.lat,
  lng: @bounty.lng
  }) %>
